# Changelog

The repository-root `CHANGELOG.md` is the authoritative release history. It is included below so
the website cannot silently diverge from the file shipped with the source.

```{include} ../CHANGELOG.md
:start-after: "# Changelog"
```
